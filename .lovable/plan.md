

## Plan: Reduce dead space above "See What's Inside" scroll indicator

### Change in `src/components/landing/Hero.tsx`

Move the scroll indicator button upward by changing its positioning from `bottom-10` to `bottom-20` (or similar), reducing the gap between the CTA content and the scroll prompt.

