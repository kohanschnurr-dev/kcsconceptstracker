Rename the diamond legend label from "Milestone" to "Key Event".

**File:** `src/components/calendar/CalendarLegend.tsx` line 15 — change `Milestone` → `Key Event`. The orange "Milestones" group label (the colored swatch) stays as-is.